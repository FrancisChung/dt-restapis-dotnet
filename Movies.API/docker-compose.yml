version : "3.9"

services:
  db:
    image: postgres:latest
    restart: always
    environment:
        - POSTGRES_USER=course
        - POSTGRES_PASSWORd=changeme
        - POSTGRES_DB=mvoies
    ports:
        - '5432:5432'